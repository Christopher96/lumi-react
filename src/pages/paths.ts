enum Paths {
  TOP_BAR = "/",
  HOME = "/home",
  START = "/start",
  LOADING = "/loading",
  INVITE = "/invite",
  SETTINGS = "/settings",
  LEAVE = "/leave",
  SERVER_LOG = "/serverlog",
}

export default Paths;
