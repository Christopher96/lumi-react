{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && tsc",
    "start": "npm run build && electron ./dist/electron/src/index.js",
    "electron:build": "npm run build && electron-builder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.12.21",
    "@types/socket.io-client": "^1.4.32",
    "electron": "^8.2.5",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.1",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "lumi-cli": "0.0.37"
  }
}
