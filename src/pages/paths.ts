enum Paths {
  TOP_BAR = "/",
  HOME = "/home",
  START = "/start",
  LOADING = "/loading",
  INVITE = "/invite",
  SETTINGS = "/settings",
  LEAVE = "/leave",
  ROOM_FOLDER = "/roomfolder",
}

export default Paths;
