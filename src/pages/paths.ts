enum Paths {
  TOP_BAR = "/",
  HOME = "/home",
  START = "/start",
  LOADING = "/loading",
  INVITE = "/invite",
  SETTINGS = "/settings",
  LEAVE = "/leave",
  ROOM = "/room",
  SERVER_LOG = "/serverlog",
}

export default Paths;
